# SMTP config
SMTP_HOST=mail.example.com         # Mailserver-Host
SMTP_PORT=465                      # SMTP-Port (465 für SMTPS)
SMTP_USER=todo@example.com         # SMTP-Benutzername
SMTP_PASS=your_smtp_password       # SMTP-Passwort
SMTP_FROM=todo@example.com         # Absenderadresse

# Database
DATABASE_URL=postgresql://user:pass@host/db?sslmode=require  # PostgreSQL-Verbindungs-URL

# Secrets
COOKIE_SECRET=your_cookie_secret   # Secret für Session-Cookies
JWT_SECRET=your_jwt_secret         # Secret für JWTs

# Server config
PORT=3001                          # Backend-Port
FRONTEND_URL=http://localhost:3000 # URL des Frontends (für CORS etc.)
